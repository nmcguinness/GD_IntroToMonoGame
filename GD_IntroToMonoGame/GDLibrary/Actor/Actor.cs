namespace GDLibrary
{
    public class Actor
    {
        private string id;
       // private string description;
        //private int type; //enum {camera, prop, arch, player, npc}

        public Actor(string id)
        {
            this.id = id;
        }
    }
}
